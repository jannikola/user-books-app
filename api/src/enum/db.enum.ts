export enum ColumnType {
    INT = "int",
    VARCHAR = "varchar",
    TIMESTAMP_TIMEZONE = "timestamptz",
}

export enum GenerationStrategy {
    INCREMENT = "increment",
}

export enum FKDelete {
    CASCADE = "CASCADE",
}
